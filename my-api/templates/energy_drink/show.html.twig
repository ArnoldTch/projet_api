{% extends 'base.html.twig' %}

{% block title %}EnergyDrink{% endblock %}

{% block body %}
    <h1>EnergyDrink</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ energy_drink.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ energy_drink.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ energy_drink.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_energy_drink_index') }}">back to list</a>

    <a href="{{ path('app_energy_drink_edit', {'id': energy_drink.id}) }}">edit</a>

    {{ include('energy_drink/_delete_form.html.twig') }}
{% endblock %}
